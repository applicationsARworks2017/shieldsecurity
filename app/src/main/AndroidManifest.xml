<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mars.gateshield">

    <application
        android:allowBackup="true"
        android:icon="@drawable/shield"
        android:label="@string/app_name"
        android:roundIcon="@drawable/shield"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.mars.gateshield.SpalshScreen">

        </activity>
        <activity android:name="com.mars.gateshield.Activity.LoginActivity" />
        <activity android:name="com.mars.gateshield.Activity.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>